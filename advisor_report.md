# h2track-xian 项目阶段汇报

## 1. 项目背景与目标
本项目面向“氢气泄漏巡检与源头追踪”场景，目标是在仿真环境中搭建一套完整的机器人闭环系统，为后续算法研究和实物验证打基础。

目标机器人平台为二轮差速移动机器人，搭载：
- 激光雷达，用于环境感知、避障与导航
- 氢气传感器，用于泄漏检测与追踪

希望逐步实现的能力链条为：
1. 在已知环境中完成自主巡检
2. 发现氢气后从巡检模式切换到追踪模式
3. 在追踪过程中继续保持避障与规划运动能力
4. 最终定位泄漏源附近并输出告警/估计结果

当前工作的重点不是一步到位做最复杂的算法，而是先把“仿真底座 + 导航 + 气体感知 + 模式切换 + 闭环验证”搭起来。

## 2. 项目发展规划
### 第一阶段：建立可运行的标准仿真底座
第一阶段的目标是解决“系统能不能稳定跑起来”的问题。

主要内容包括：
- 基于 `ROS 2 Humble + Gazebo Classic + Nav2` 建立仿真工作区
- 构建二轮差速机器人模型、地图、场景、雷达与底盘控制链路
- 实现巡检与追踪的状态机框架
- 先使用简化气体场模型验证任务逻辑，而不一开始就引入复杂流体仿真

这一阶段的核心价值是：先把系统架构边界、接口和运行链路理顺。

### 第二阶段：引入更真实的 GADEN 气体仿真
第二阶段的目标是把简化气体场替换成更真实的气体扩散/回放链路，同时尽量不重写上层任务逻辑。

主要内容包括：
- 使用外部 `gaden_ws` 工作区承载 GADEN 依赖
- 保留主工程 `h2track-xian` 的独立性
- 通过 adapter 将 GADEN 传感器数据统一转换为 `/gas_concentration`
- 保留 `use_gaden:=false/true` 切换能力，便于 A/B 对比

这一阶段的核心价值是：在不破坏上层系统边界的情况下，提升气体仿真的真实性。

### 第三阶段：算法增强与指标评估
后续建议的重点方向包括：
- 从当前基于局部趋势的追踪策略，逐步升级为更稳定的源定位方法
- 做多组场景下的成功率、定位误差、时间开销统计
- 加入不同风向、不同障碍布局、不同起点等实验工况
- 评估 GADEN 模式与简化模型模式下的行为差异

### 第四阶段：面向实物迁移
如果仿真闭环稳定，后续可以进一步向实物平台迁移：
- 替换为真实雷达、真实气体传感器
- 将仿真中的接口保持不变，降低迁移成本
- 解决实际定位、传感器噪声、时钟同步与安全策略问题

## 3. 当前已经完成的工作
目前已经在纯 ASCII 路径下重建了项目工作区：
- 项目目录：`/home/user/h2track-xian`
- 外部 GADEN 工作区：`/home/user/gaden_ws`

当前系统已经形成两层清晰结构：

### 3.1 仿真与导航层
`h2track_sim` 负责：
- Gazebo 场景与世界文件
- 机器人 URDF/Xacro
- 地图、RViz 配置
- Nav2 启动与参数配置
- 主 bringup 启动链

### 3.2 任务与气体处理层
`h2track_tracking` 负责：
- 简化气体场模型与节点
- 巡检/追踪状态机逻辑
- 任务管理节点 `mission_manager_node`
- GADEN 气体传感器适配节点 `gaden_adapter_node`
- 新增的 TF 门控节点 `gaden_sensor_gate_node`

### 3.3 当前已具备的能力
当前项目已经可以做到：
- `use_gaden:=false` 时，用简化气体场驱动整套巡检/追踪逻辑
- `use_gaden:=true` 时，用 GADEN playback + adapter 提供气体浓度输入
- Nav2 能正常激活，机器人可以进入导航执行状态
- 追踪系统仍然以 `/gas_concentration` 为统一输入接口
- GADEN 传感器不再裸启动，而是先等 TF 链连通后再启动

### 3.4 当前验证结果
目前已经完成的验证包括：
- `pytest`：`21 passed`
- `colcon build`：成功
- `use_gaden:=false`：可启动并进入导航运行
- `use_gaden:=true`：GADEN 主链路可启动并进入导航运行

## 4. 项目过程中遇到的主要问题

### 4.1 中文路径导致环境不稳定
最早项目位于中文路径目录下，这在 ROS、Gazebo、第三方依赖和脚本调用场景中会带来潜在问题，包括：
- 路径处理不统一
- 外部工具兼容性变差
- 调试成本上升

#### 解决思路
没有继续在旧目录上修修补补，而是直接在纯 ASCII 路径下重建项目：
- 旧目录保留为参考
- 新项目统一迁移到 `/home/user/h2track-xian`

#### 经验
对 ROS 生态和第三方工具链来说，路径规范不是小问题，越早统一越省成本。

### 4.2 Nav2 启动失败，行为树插件不完整
在接入 Nav2 时，`bt_navigator` 一度无法正常激活，报错集中在：
- `ComputePathThroughPoses`
- `RemovePassedGoals`
等行为树节点无法识别。

#### 问题本质
不是“导航算法坏了”，而是 Nav2 Humble 的行为树 XML 与插件库配置不匹配。

#### 解决思路
采用系统化排查而不是盲改：
1. 先重现实验现象
2. 读清错误信息和默认 BT XML
3. 对照官方 Humble 默认插件列表
4. 补齐 `bt_navigator` 所需插件与默认 BT 路径

#### 经验
ROS 2 多数“起不来”的问题，根因往往在配置与依赖关系，而不是业务算法本身。

### 4.3 GADEN 集成时，如何避免上层逻辑被第三方格式绑死
如果让任务层直接依赖 GADEN 内部结果文件、原始消息格式或工作流，后面会非常难维护。

#### 解决思路
采用“适配层隔离”的方式：
- GADEN 只负责提供更真实的气体场或传感器数据
- `gaden_adapter_node` 将其统一转成 `/gas_concentration`
- `mission_manager_node` 继续只关心 `/gas_concentration`

#### 经验
外部研究型工具应当被隔离在系统边界外，上层任务逻辑只依赖稳定接口。

### 4.4 Gazebo、AMCL、TF、气体传感器之间的启动时序问题
在第二阶段中，最明显的问题是：
- 仿真刚启动时 TF 树尚未建立完整
- `simulated_gas_sensor` 如果过早启动，会因为 `gaden_map -> base_link` 不连通而报错

早期做法是简单延时，但延时本质上只是“猜时间”，并不稳。

#### 解决思路
最终引入了一个独立门控节点：`gaden_sensor_gate_node`。

它的职责是：
- 轮询检测 `gaden_map -> base_link` 是否已经可查询
- 只有在 TF 连通后，才真正启动 `simulated_gas_sensor`
- 如果超时仍不连通，则明确报错退出，而不是继续模糊运行

#### 经验
对 ROS 多组件系统，解决时序问题的正确思路通常不是一味加固定延时，而是“等条件满足后再继续”。

### 4.5 真实系统问题与第三方节点问题需要分开看
即便在门控之后，第三方 `simulated_gas_sensor` 在首帧仍可能出现一次“时间外插”类警告，但它已经不再是之前那种 TF 断树导致的启动失败。

#### 解决思路
将问题拆成两类：
- 系统级问题：如 TF 是否连通、节点是否能启动、导航是否激活
- 第三方局部问题：如某个传感器节点首帧时间戳的历史缓存不足

#### 经验
要避免把所有错误混在一起看，否则会误判系统整体状态。

## 5. 做这个项目时形成的思考方法
在整个过程中，我目前比较明确的一条思路是：

### 5.1 先把系统边界理清，再做真实性增强
如果一开始就追求最复杂的 CFD、最复杂的源定位算法，系统很容易卡死在集成细节上。

因此采用的顺序是：
- 先做可运行闭环
- 再替换更真实的气体来源
- 最后再增强算法

### 5.2 把“导航问题”和“追踪问题”分开
当前架构中，Nav2 负责“怎么安全到达目标”；追踪策略负责“下一步应该往哪走”。

这能明显降低耦合度，也更方便后续独立优化。

### 5.3 遇到问题先找根因，而不是反复试参数
这次项目里比较典型的经验是：
- Nav2 问题不能只靠改阈值
- GADEN 传感器问题不能只靠无限加延时
- 启动失败必须先看日志、看 TF、看节点依赖关系

### 5.4 保留回退路径非常重要
`use_gaden:=false/true` 的双模式非常有价值：
- 可以快速比较“简化场”和“GADEN 场”
- 遇到问题时能定位是上层逻辑还是 GADEN 集成问题
- 不会因为第二阶段未完全稳定而失去第一阶段成果

## 6. 当前阶段的经验总结
可以总结出几条比较清晰的经验：

1. 做机器人系统集成时，架构设计的重要性不低于算法设计。
2. ROS 2 项目中，大量问题来自组件之间的接口、时序和配置，而不是单个节点内部逻辑。
3. 第三方工具接入时，必须有中间适配层，否则后期维护会很困难。
4. 先建立一个可运行、可验证、可回退的系统，比一开始追求最复杂方案更稳妥。
5. 测试和回归验证要尽量前置，否则修一个问题时很容易引入另一个问题。

## 7. 当前仍然存在的不足
虽然系统已经基本搭建起来，但仍有一些问题还需要后续继续推进：

- 当前追踪策略仍属于“工程可运行版本”，还不是最终研究型算法
- 导航控制参数还有优化空间，机器人在某些路径上仍可能出现局部进展缓慢
- GADEN 传感器在首帧仍可能出现一次时间外插类警告，需要进一步抑制
- 还缺少系统性的实验数据统计与图表化结果输出

## 8. 下一步建议
接下来建议按以下顺序推进：

### 8.1 先稳定系统运行质量
- 继续优化 Nav2 参数
- 进一步压低 GADEN 首帧时间外插警告
- 增加更多运行日志与可视化输出

### 8.2 再做实验指标化
- 统计巡检成功率
- 统计追踪成功率
- 统计源点定位误差
- 统计任务完成时间

### 8.3 最后再升级算法
- 在当前状态机和接口不变的基础上，升级泄漏源追踪算法
- 对比简化场与 GADEN 场下的性能差异
- 为后续实物迁移做准备

## 9. 现阶段结论
目前项目已经从“概念设想”进入到了“系统原型可运行”的阶段。

已经完成的关键跨越是：
- 从旧工程迁移到干净、稳定的新工作区
- 建立了标准 ROS 2 / Gazebo / Nav2 仿真底座
- 实现了巡检、追踪、GADEN 适配和 TF 门控启动机制
- 把系统从“能写代码”推进到了“能实际跑闭环”

因此，现阶段最适合向导师汇报的定位是：
**项目已经完成了仿真系统主框架搭建与第二阶段 GADEN 接入，当前正在从“功能可用”继续推进到“运行更稳、指标更清晰、算法更强”的阶段。**
